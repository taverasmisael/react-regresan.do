import React, { PureComponent } from 'react'

export default class Branches extends PureComponent {
  render() {
    return (
      <React.Fragment>
        Branches
      </React.Fragment>
    )
  }
}
